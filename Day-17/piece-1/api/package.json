{
  "name": "quotes-mi-broker",
  "version": "1.0.0",
  "description": "SWA linked-backend Azure Functions broker — forwards /api/quotes to the Week-1 API using a system-assigned Managed Identity (no secret).",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@azure/identity": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "@types/node": "^20.0.0"
  }
}
